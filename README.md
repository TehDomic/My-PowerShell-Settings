# My PowerShell Settings

## AutoInstall.ps1 Script

### Option 1: Windows Terminal Setup

Auto-Install will:
- Install Windows Terminal only if necessary.
- Set Windows Terminal as default only when running on Windows 11.
- Skip default terminal setup entirely for Windows 10 users, with clear instructions to launch Windows Terminal manually as needed.